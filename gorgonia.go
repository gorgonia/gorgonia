package gorgonia

import "gorgonia.org/gorgonia/internal/engine"

// Graph ...
type Graph struct {
	*engine.ExprGraph
}

// Add a and b and create a new node
func Add(a, b *engine.Node) (*engine.Node, error) {
	return nil, nil
}
